#include <stdio.h>
#define SIZE 10;
int main () {
	// In a new C file declare small arrays of different
	// primitive types: char, int, float and double
	int* buckets = (int[SIZE]){0};				  // buckets to count how many of each number will be in the array below
	return 0;
};
